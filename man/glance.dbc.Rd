% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{glance.dbc}
\alias{glance.dbc}
\title{Glance at a dbc object}
\usage{
\method{glance}{dbc}(x, ...)
}
\arguments{
\item{x}{A \code{dbc} object.}

\item{...}{Currently unused.}
}
\description{
Returns a one-row data frame of model-level summary statistics. Compatible
with \code{\link[broom:reexports]{broom::glance()}} and \code{modelsummary::modelsummary()}.
}
\details{
\code{fe_panel} is always \code{"Yes"} because the DBC estimator absorbs individual
fixed effects via within-demeaning. \code{fe_time} is \code{"No"} because time fixed
effects are not automatically absorbed; include time dummies as covariates
if needed.
}
\examples{
set.seed(1)
dat <- DGP(N = 100, N_T = 4, rho1 = 0.3, rho2 = 0.2, tau = 0.5)
fit <- dbc(
    outcome_fml   = y ~ lag_y + D,
    treatment_fml = D ~ lag_y,
    lag_y = "lag_y", treatment = "D",
    panel_id = "panel_id", time_id = "time",
    data = dat
)
broom::glance(fit)
}
