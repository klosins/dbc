% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbc.R
\name{dbc}
\alias{dbc}
\title{Dynamic Bias Correction (DBC) Estimator}
\usage{
dbc(
  outcome_fml,
  treatment_fml = NULL,
  lag_y,
  lag_d = character(0),
  treatment,
  panel_id,
  time_id,
  data,
  balance_type = "shared.times",
  reltol = 1e-12,
  maxit = 10000
)
}
\arguments{
\item{outcome_fml}{Formula for the outcome equation. No fixed effects (no |).
Example: y ~ lag_y1 + D + X1 + D:W1}

\item{treatment_fml}{Optional formula for the treatment equation. If NULL,
treatment is assumed exogenous (\eqn{\rho_2 = 0}{rho_2 = 0}). Example: D ~ lag_y1 + X2}

\item{lag_y}{Character vector of lagged outcome variable names in the outcome
equation, ordered lag-1, lag-2, ... Currently only length-1 is supported.
Example: "lag_y1", or c("lag_y1", "lag_y2") (latter errors until implemented).}

\item{lag_d}{Character vector of lagged treatment variable names in the
treatment equation. Currently not supported (errors if non-empty).}

\item{treatment}{Character: name of the treatment variable.}

\item{panel_id}{Character: name of the panel unit identifier.}

\item{time_id}{Character: name of the time variable.}

\item{data}{A data.frame or data.table.}

\item{balance_type}{Character: passed to plm::make.pbalanced(). Default "shared.times".}

\item{reltol}{Double: Relative convergence tolerance of GMM objective function.}

\item{maxit}{Double: Maximum number of solver iterations.}
}
\value{
An object of class "dbc".
}
\description{
Implements the bias-corrected GMM estimator from Klosin (2024) for
dynamic panel models with fixed effects. Corrects for both Nickell bias
and dynamic bias.
}
\examples{
set.seed(1)

# Exogenous treatment
dat_exog <- DGP(N = 100, N_T = 4, rho1 = 0.3, rho2 = 0, tau = 0.5)
fit_exog <- dbc(
    outcome_fml = y ~ lag_y + D,
    lag_y       = "lag_y",
    treatment   = "D",
    panel_id    = "panel_id",
    time_id     = "time",
    data        = dat_exog
)
print(fit_exog)

# Endogenous treatment
dat_endo <- DGP(N = 100, N_T = 4, rho1 = 0.3, rho2 = 0.2, tau = 0.5)
fit_endo <- dbc(
    outcome_fml   = y ~ lag_y + D,
    treatment_fml = D ~ lag_y,
    lag_y         = "lag_y",
    treatment     = "D",
    panel_id      = "panel_id",
    time_id       = "time",
    data          = dat_endo
)
print(fit_endo)

# Heterogeneous treatment effects (interactions)
dat_int <- DGP(N = 100, N_T = 4, rho1 = 0.3, rho2 = 0.2,
               tau = c(0.8, 1.2), n_W = 2)
fit_int <- dbc(
    outcome_fml   = y ~ lag_y + D:W_1 + D:W_2,
    treatment_fml = D ~ lag_y,
    lag_y         = "lag_y",
    treatment     = "D",
    panel_id      = "panel_id",
    time_id       = "time",
    data          = dat_int
)
print(fit_int)
}
