% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{parse_formulas}
\alias{parse_formulas}
\title{Parse formulas using terms() and the factors attribute}
\usage{
parse_formulas(outcome_fml, treatment_fml, lag_y, lag_d, treatment, data)
}
\arguments{
\item{lag_y}{Character vector of lagged outcome variable names,
ordered lag-1, lag-2, ... (currently only length-1 is supported).}

\item{lag_d}{Character vector of lagged treatment variable names
in the treatment equation (currently only length-0 is supported).}
}
\description{
Classifies each term in the outcome equation as:
\itemize{
\item rho_terms: lag_y main effects (one per lag, ordered lag-1, lag-2, ...)
\item tau: any term involving treatment (bare D, D:W interactions)
\item beta1: everything else (exogenous controls, their interactions, etc.)
}
}
\details{
For the treatment equation:
\itemize{
\item rho2_terms: lag_y terms (one per Y-lag present in treatment equation)
\item lag_d_terms: lag_d terms (D lags in treatment equation, placeholder)
\item beta2: everything else
}

Uses attr(terms(), "factors") — a matrix where rows = variables,
cols = terms, entries > 0 indicate involvement. No regex needed.
}
\keyword{internal}
