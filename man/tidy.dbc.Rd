% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{tidy.dbc}
\alias{tidy.dbc}
\title{Tidy a dbc object into a coefficient table}
\usage{
\method{tidy}{dbc}(x, conf.int = FALSE, conf.level = 0.95, ...)
}
\arguments{
\item{x}{A \code{dbc} object.}

\item{conf.int}{Logical: include confidence interval columns? Default FALSE.}

\item{conf.level}{Confidence level for intervals. Default 0.95.}

\item{...}{Currently unused.}
}
\description{
Returns one row per parameter with columns \code{term}, \code{estimate}, \code{std.error},
\code{statistic}, \code{p.value}, and \code{group}. The \code{group} column labels each row
as \code{"Outcome"} or \code{"Treatment"}, enabling vertically stacked equation
subpanels in \code{modelsummary::modelsummary()} via
\code{shape = group + term ~ model}.
}
\examples{
set.seed(1)
dat <- DGP(N = 100, N_T = 4, rho1 = 0.3, rho2 = 0.2, tau = 0.5)
fit <- dbc(
    outcome_fml   = y ~ lag_y + D,
    treatment_fml = D ~ lag_y,
    lag_y = "lag_y", treatment = "D",
    panel_id = "panel_id", time_id = "time",
    data = dat
)
broom::tidy(fit)
broom::tidy(fit, conf.int = TRUE, conf.level = 0.95)
}
